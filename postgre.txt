sudo -i -u postgres
psql                    // TOGETHER: sudo -u postgres psql

\q                      //to exit

\dt                     //to show tables
\password               // enter: web, USEFUL ONLY THE FIRST TIME TO CONNECT THE DB TO THE SERVER


CREATE DATABASE test;
DROP table;              //to delete already made table
\c test;                 //connect to test db
\d                       //to see the tables

create table users(id serial primary key, email text unique NOT NULL, password text NOT NULL, username text unique NOT NULL);
insert into users(email, password, username) values ('savvas@gmail.com', 'savvas', 'savvikos'); 
//insert at terminal because the other passwords will be hashed and with restrictions, i want to use as eg


select * from users;