sudo -i -u postgres
psql                    // TOGETHER: sudo -u postgres psql

\q                      //to exit

\dt                     //to show tables
\password               // enter: web, USEFUL ONLY THE FIRST TIME TO CONNECT THE DB TO THE SERVER


CREATE DATABASE test;
DROP table;              //to delete already made table
\c test;                 //connect to test db
\d                       //to see the tables

create TABLE entries (user_id integer NOT NULL, info json NOT NULL);
create table newusers(id serial primary key, email text unique NOT NULL, password text NOT NULL, username text unique NOT NULL, info json, count integer DEFAULT 0, last_entry timestamp, admin_pass integer DEFAULT 0);

//to insert admin into newusers
INSERT INTO newusers(email, password, username, admin_pass) VALUES('admin','$2a$10$L4D0aAZANZwWZjEVLkesJulO.RrUaVUHEdNFrnJLPGAITvrdOS10O','admin', 1);

select * from table_name where false;   //name of columns


select * from users;
